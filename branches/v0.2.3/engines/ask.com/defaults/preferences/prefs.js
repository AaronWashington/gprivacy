// These standard preferences must be named  'extensions.gprivacy.engines.<ID>.*'
// So please @change .ask. to .yourID. !
pref("extensions.gprivacy.engines.ask.enabled",   true);  // engine active
pref("extensions.gprivacy.engines.ask.allevts",   false); // remove ALL events from links (careful!)
pref("extensions.gprivacy.engines.ask.own",       true);  // Allow tracking on same page (helps cleaning up cluttering)
